export class AStar {
    constructor(allPoints, startPoint, pointsOfInterest) {
        this._allPoints = allPoints;
        this._startPoint = startPoint;
        this._pointsOfInterest = pointsOfInterest;
    }
    findPath() { }
}
